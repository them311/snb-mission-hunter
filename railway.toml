[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'uvicorn api:app --host 0.0.0.0 --port ${PORT:-8000} & python main.py'"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
